import "./AnnouncementBar.css";

function AnnouncementBar() {
  return (
    <div className="announcement">
      <div className="announcementTrack">
        <span>archive era — limited quantities — no restocks</span>
        <span>archive era — limited quantities — no restocks</span>
        <span>archive era — limited quantities — no restocks</span>
      </div>
    </div>
  );
}

export default AnnouncementBar;
